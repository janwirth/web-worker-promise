{
  "name": "web-worker-rpc",
  "type": "module",
  "private": true,
  "workspaces": [
    "worker-rpc",
    "example"
  ],
  "scripts": {
    "build": "bun run --filter @janwirth/worker-rpc build",
    "prepublishOnly": "bun run build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
