{
  "name": "web-worker-rpc",
  "type": "module",
  "private": true,
  "workspaces": [
    "web-worker-promise",
    "example"
  ],
  "scripts": {
    "build": "bun run --filter @janwirth/web-worker-promise build",
    "prepublishOnly": "bun run build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
